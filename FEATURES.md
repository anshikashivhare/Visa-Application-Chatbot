# Features

## Current (Implemented)
- Static frontend: `index.html` with basic content and structure.
- Styling: `styles.css` for layout and responsive basics.
- Client JS: `script.js` for any interactive behavior.
- Optional Node server: `server.js` (serves static files / local testing).
- Asset folder: `assets/` for images and media.

## Development / Planned
- Improved responsive layout and accessibility (WCAG basics).
- Form flow for guided visa application steps.
- Client-side validation and persistence (localStorage).
- Server-side endpoints for storing submissions.
- Internationalization (multi-language support).
- Automated tests (unit + integration) and CI integration.

## Notes
- Status labels: implemented, planned, optional.
- To mark progress, update this file and the project TODOs.
